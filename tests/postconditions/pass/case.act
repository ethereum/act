constructor of C
interface constructor()

creates
   uint x := 0
   uint y := 0
	
behaviour foo of C
interface foo(uint flag, uint z)

iff
   CALLVALUE == 0

case flag == 0:

storage
  y => z

case flag >= 5 :

storage
  y => 0

case flag >= 3:

storage
  y => z + 2

case flag >= 1:

storage
  y => z + 1

ensures 

  flag == 4 => post(y) == z + 2
