behaviour create_a of B
interface create_a()

iff
   CALLVALUE == 0

storage
// newAddr should be a builtin, but let's just a free variable
// c for now
   a => c

// storage labels can also introduce new contracts
creates storage c
   uint x := 1

