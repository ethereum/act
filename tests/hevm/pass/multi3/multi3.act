// contract A

constructor of A
interface constructor(uint z)

iff
   CALLVALUE == 0
   inRange(uint256, z + 1)

creates
   uint x := z + 1


behaviour x of A
interface x()

iff
   CALLVALUE == 0

returns
   pre(x)


// contract B

constructor of B
interface constructor(uint z)
iff
   CALLVALUE == 0
   inRange(uint256, z + 42)

creates
   uint x := z + 42


// contract C
constructor of C
interface constructor(uint u)

iff
   CALLVALUE == 0

creates
   uint y := 0
   A a := create A(u)
   B b := create B(u) 